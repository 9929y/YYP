---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="case-stat rv" data-reveal="text">
  <strong>{value}</strong>
  <span>{label}</span>
</div>
